// Looping Triangle

   // Метод While

var star = "*"
while (star.length <= 8) {
    console.log(star);
    star += "*";
}

   // Метод For

// for (var i = "*"; i.length <= 7; i += "*")
//     console.log(i)